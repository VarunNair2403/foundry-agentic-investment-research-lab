# Copy this file to .env for local development.
# Never commit .env or real credentials.

APP_ENV=development
LOG_LEVEL=INFO

# Version 1 uses a free deterministic mock provider.
LLM_PROVIDER=mock

# Add only after configuring a live Azure AI Foundry model.
AZURE_AI_ENDPOINT=
AZURE_AI_API_KEY=
AZURE_AI_DEPLOYMENT_NAME=